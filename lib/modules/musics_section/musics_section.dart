import 'package:flutter/material.dart';

class MusicsSection extends StatelessWidget {
  const MusicsSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}